#ifndef MAIN_HPP
#define MAIN_HPP

#include <iostream>
#include <cstdlib>
#include <string>
#include <vector>
#include "log.hpp"

const std::string PROG_NAME = "Project Euler";
const std::string PROG_DESC = "Problem 1: Sum of multiples of 3 and 5";
const std::string VERSION = "v1.1";

const std::string PROMPT = "Enter a maximum number to calculate up to (q to quit): ";

#endif
