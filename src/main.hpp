#ifndef MAIN_HPP
#define MAIN_HPP

#include <iostream>
#include <cstdlib>
#include <string>
#include <vector>
#include "log.hpp"

const std::string PROG_NAME = "Project Euler";
const std::string PROG_DESC = "Problem 2: Sum of even Fibonacci numbers";
const std::string VERSION = "v2.1";

const std::string PROMPT = "Enter a maximum number to calculate up to (q to quit): ";

#endif
